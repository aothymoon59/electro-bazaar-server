export interface IQuery {
  minPrice: string;
  maxPrice: string;
  releaseDate: string;
  storage: number;
  cameraResolution: number;
  brand: string;
  modelNumber: string;
  category: string;
  operatingSystem: string;
  connectivity: string;
  powerSource: string;
}
